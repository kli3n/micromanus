// Flat ESLint config placeholder.
// The full Next lint preset (eslint-config-next) is not a Phase-1 dependency, so
// keep an empty flat config: `next lint` becomes a no-op instead of erroring on a
// missing config. A later phase can add the preset without renaming this file.
export default [];
