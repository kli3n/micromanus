{
  "name": "micromanus",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "test:rls": "node --env-file-if-exists=.env.local scripts/rls-probe.ts",
    "test:money": "node --env-file-if-exists=.env.local scripts/debit-concurrency.ts",
    "test:guard": "node --env-file-if-exists=.env.local scripts/auth-guard.test.ts",
    "eval:offline": "node --env-file-if-exists=.env.local scripts/eval-run.ts",
    "eval:judge": "node --env-file-if-exists=.env.local scripts/eval-judge.ts",
    "eval:gate": "npm test && npm run typecheck && npm run test:rls",
    "audit:tokens": "node --env-file-if-exists=.env.local scripts/audit-tokens.ts",
    "audit:css": "node --env-file-if-exists=.env.local scripts/audit-css-classes.ts",
    "audit:contrast": "node --env-file-if-exists=.env.local scripts/audit-contrast.ts",
    "audit:a11y:deployed": "node --env-file-if-exists=.env.local scripts/a11y-audit.ts",
    "audit:gate": "npm test && npm run typecheck && npm run build && npm run audit:tokens && npm run audit:css && npm run audit:contrast"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "0.112.3",
    "@mozilla/readability": "0.6.0",
    "@sparticuz/chromium": "149.0.0",
    "@supabase/ssr": "0.12.3",
    "@supabase/supabase-js": "2.110.7",
    "@vercel/functions": "3.7.5",
    "linkedom": "0.18.13",
    "next": "16.2.10",
    "openai": "6.48.0",
    "puppeteer-core": "^25",
    "react": "^19",
    "react-dom": "^19",
    "react-markdown": "10.1.0",
    "remark-gfm": "4.0.1",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "4.3.3",
    "@types/node": "^24",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "tailwindcss": "4.3.3",
    "typescript": "^5",
    "vitest": "^3"
  }
}
