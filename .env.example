# MicroManus — environment variables (copy to .env.local; never commit real values)
# Public (safe in the browser — RLS enforces access):
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-or-publishable-key

# Server-only (reserved for Phase 2 — never prefix with NEXT_PUBLIC_):
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-or-secret-key
# ENCRYPTION_KEY=32-byte-aes-256-gcm-key
