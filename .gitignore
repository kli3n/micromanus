# dependencies
node_modules/
.pnp
.pnp.*
docs/*
!docs/adr/
# next.js
.next/
/out/
/build
next-env.d.ts
*.tsbuildinfo
design/BROWSER-DESIGN.md
# testing
/coverage
# Lighthouse a11y reports (D-69): a report can embed request headers and the
# audit runs against a signed-in session — evidence to read, never to commit.
.a11y/

# env files — no secrets in repo; keep only the example
.env*
!.env.example

# planning + agent config (private, never pushed)
.planning/
.claude/

# vercel
.vercel

# supabase (CLI local state created by `supabase link` / `db push` in Gate B)
supabase/.temp
supabase/.branches
supabase/.env

# misc
.DS_Store
*.pem
npm-debug.log*
