# dependencies
node_modules/
.pnp
.pnp.*
docs/
# next.js
.next/
/out/
/build
next-env.d.ts
*.tsbuildinfo
design/BROWSER-DESIGN.md
# testing
/coverage

# env files — no secrets in repo; keep only the example
.env*
!.env.example

# planning + agent config (private, never pushed)
.planning/
.claude/

# vercel
.vercel

# supabase (CLI local state created by `supabase link` / `db push` in Gate B)
supabase/.temp
supabase/.branches
supabase/.env

# misc
.DS_Store
*.pem
npm-debug.log*
